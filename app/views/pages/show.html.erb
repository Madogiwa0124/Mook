<div id="show" class="container">
  <div class="detail-area">
    <h1>
      <%= render "favorite_form", { page: @page } %>
      <%= @page.name %>
    </h1>
    <div class="content-area">
      <p class="url">URL：<%= link_to @page.url, @page.url %></p>
      <p class="tags_area">
        <% @page.tag_list.each do |tag| %>
          <%= link_to tag, search_pages_path(search_text: tag), class: "label label-primary" %>
        <% end %>
      </p>
      <div class="btn-area">
        <%= link_to '編集', edit_page_path(@page), class: "btn btn-info" %>
        <%= link_to '戻る', pages_path, class: "btn btn-warning" %>
      </div>
    </div>
  </div>
</div>